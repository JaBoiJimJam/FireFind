# Manual QA Summary – 2025-05-09

## Context
- Backend commit: `HEAD` during FireFind QA dry-run.
- Rules source: `backend/samples/rules.minimal.yaml` (imported through admin UI).
- Environment: macOS 14.5, Python 3.11.7, Node 18.19.0, Chrome 124.

## Checklist Execution
- [x] Environment prepared via `./start_dev.sh` with sample configuration paths.
- [x] Rule authoring workflow exercised (create, edit, toggle, delete) with no
      validation regressions observed.
- [x] YAML export from admin UI matched `GET /api/config/rules` payload when
      diffed after normalising with `yq`.
- [x] Re-imported YAML snapshot restored identical cards and validation status.
- [x] Verified `RulesConfig.get_legacy_mapping()` returns list-based port fields
      for downstream automation.
- [x] Ran `backend/run_backend.py` against `backend/samples/generic_sample.csv`
      to ensure reports generated without schema mismatches.

## Notable Observations
- Exported YAML retained analyzer severity overrides and nested condition
  groups, matching backend expectations.
- Local storage cache cleared automatically after deleting the final rule,
  preventing stale data on reload.
- CLI execution reused the updated thresholds without requiring a backend
  restart, confirming runtime reload behaviour.

## Outcome
All manual QA gates defined in `reports/manual-qa-runbook.md` passed. Release
candidates based on this commit are ready for stakeholder sign-off.
